naftiko: 1.0.0-alpha2
info:
  label: Wise Platform API — sca-sessions
  description: 'Wise Platform API — sca-sessions. 1 operations. Lead operation: Create a SCA session. Self-contained Naftiko
    capability covering one Wise business surface.'
  tags:
  - Wise
  - sca-sessions
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    WISE_API_KEY: WISE_API_KEY
capability:
  consumes:
  - type: http
    namespace: platform-sca-sessions
    baseUri: https://api.wise.com
    description: Wise Platform API — sca-sessions business capability. Self-contained, no shared references.
    resources:
    - name: v2-profiles-profileId-sca-sessions-authorise
      path: /v2/profiles/{profileId}/sca-sessions/authorise
      operations:
      - name: scasessioncreate
        method: POST
        description: Create a SCA session
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: profileId
          in: path
          type: integer
          description: The profile ID.
          required: true
    authentication:
      type: bearer
      token: '{{env.WISE_API_KEY}}'
  exposes:
  - type: rest
    namespace: platform-sca-sessions-rest
    port: 8080
    description: REST adapter for Wise Platform API — sca-sessions. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/v2/profiles/{profileid}/sca-sessions/authorise
      name: v2-profiles-profileid-sca-sessions-authorise
      description: REST surface for v2-profiles-profileId-sca-sessions-authorise.
      operations:
      - method: POST
        name: scasessioncreate
        description: Create a SCA session
        call: platform-sca-sessions.scasessioncreate
        with:
          profileId: rest.profileId
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: platform-sca-sessions-mcp
    port: 9090
    transport: http
    description: MCP adapter for Wise Platform API — sca-sessions. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: create-sca-session
      description: Create a SCA session
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: platform-sca-sessions.scasessioncreate
      with:
        profileId: tools.profileId
      outputParameters:
      - type: object
        mapping: $.
