naftiko: 1.0.0-alpha2
info:
  label: Wise Platform API — address
  description: 'Wise Platform API — address. 5 operations. Lead operation: Get Address Requirements. Self-contained Naftiko
    capability covering one Wise business surface.'
  tags:
  - Wise
  - address
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    WISE_API_KEY: WISE_API_KEY
capability:
  consumes:
  - type: http
    namespace: platform-address
    baseUri: https://api.wise.com
    description: Wise Platform API — address business capability. Self-contained, no shared references.
    resources:
    - name: v1-address-requirements
      path: /v1/address-requirements
      operations:
      - name: addressrequirementsget
        method: GET
        description: Get Address Requirements
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: addressrequirementspost
        method: POST
        description: Get Address Requirements (with context)
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    - name: v1-addresses
      path: /v1/addresses
      operations:
      - name: addresscreate
        method: POST
        description: Create or Update an Address
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
      - name: addresslist
        method: GET
        description: List Addresses for a Profile
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: profile
          in: query
          type: integer
          description: The profile ID to list addresses for.
    - name: v1-addresses-addressId
      path: /v1/addresses/{addressId}
      operations:
      - name: addressget
        method: GET
        description: Retrieve an Address by ID
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: addressId
          in: path
          type: integer
          description: The unique identifier of the address.
          required: true
    authentication:
      type: bearer
      token: '{{env.WISE_API_KEY}}'
  exposes:
  - type: rest
    namespace: platform-address-rest
    port: 8080
    description: REST adapter for Wise Platform API — address. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/v1/address-requirements
      name: v1-address-requirements
      description: REST surface for v1-address-requirements.
      operations:
      - method: GET
        name: addressrequirementsget
        description: Get Address Requirements
        call: platform-address.addressrequirementsget
        outputParameters:
        - type: object
          mapping: $.
      - method: POST
        name: addressrequirementspost
        description: Get Address Requirements (with context)
        call: platform-address.addressrequirementspost
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/v1/addresses
      name: v1-addresses
      description: REST surface for v1-addresses.
      operations:
      - method: POST
        name: addresscreate
        description: Create or Update an Address
        call: platform-address.addresscreate
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
      - method: GET
        name: addresslist
        description: List Addresses for a Profile
        call: platform-address.addresslist
        with:
          profile: rest.profile
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/v1/addresses/{addressid}
      name: v1-addresses-addressid
      description: REST surface for v1-addresses-addressId.
      operations:
      - method: GET
        name: addressget
        description: Retrieve an Address by ID
        call: platform-address.addressget
        with:
          addressId: rest.addressId
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: platform-address-mcp
    port: 9090
    transport: http
    description: MCP adapter for Wise Platform API — address. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: get-address-requirements
      description: Get Address Requirements
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: platform-address.addressrequirementsget
      outputParameters:
      - type: object
        mapping: $.
    - name: get-address-requirements-context
      description: Get Address Requirements (with context)
      hints:
        readOnly: true
        destructive: false
        idempotent: false
      call: platform-address.addressrequirementspost
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: create-update-address
      description: Create or Update an Address
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: platform-address.addresscreate
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: list-addresses-profile
      description: List Addresses for a Profile
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: platform-address.addresslist
      with:
        profile: tools.profile
      outputParameters:
      - type: object
        mapping: $.
    - name: retrieve-address-id
      description: Retrieve an Address by ID
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: platform-address.addressget
      with:
        addressId: tools.addressId
      outputParameters:
      - type: object
        mapping: $.
